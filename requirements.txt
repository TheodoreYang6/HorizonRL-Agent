# HorizonRL-Agent Core Dependencies
# Updated: 2026-05-19 based on current stable versions
# Python >=3.10 required

# --- Agent Framework (LangGraph Ecosystem) ---
langgraph~=1.1.10              # Stateful agent orchestration
langchain~=1.2.18              # LLM application framework
langchain-core~=1.4.0          # Core abstractions
langchain-community~=0.4.1     # Community integrations
langchain-openai~=1.2.1        # OpenAI integration
langchain-anthropic~=1.4.3     # Anthropic integration
langchain-classic~=1.0.7       # Classic LangChain components
langgraph-checkpoint~=4.1.0    # Checkpointing for state persistence
langgraph-prebuilt~=1.0.13     # Prebuilt components
langgraph-sdk~=0.3.14          # SDK for LangGraph Cloud

# --- Async & Network ---
aiohttp~=3.13.5                # Async HTTP client/server
httpx>=0.27.0                  # Modern HTTP client (compatible with aiohttp)
aiofiles>=24.0.0               # Async file operations

# --- Web UI (FastAPI) ---
fastapi~=0.136.1               # Modern web framework
uvicorn[standard]~=0.47.0      # ASGI server
jinja2>=3.1.0                  # Template engine
python-multipart>=0.0.9        # Form data parsing

# --- PDF Export ---
weasyprint>=62.0
markdown>=3.7

# --- LLM Clients ---
openai~=2.36.0                 # OpenAI API client
anthropic>=0.40.0              # Anthropic API client

# --- Data & Config ---
numpy~=2.2.6                   # Numerical computing
pydantic~=2.13.4               # Data validation
pydantic-settings~=2.14.1      # Settings management
pyyaml~=6.0.3                  # YAML parser
tiktoken~=0.12.0               # Tokenization
python-dotenv>=1.0.0           # Environment variables

# --- Search & Knowledge ---
ddgs>=6.0.0
wikipedia>=1.4.0
arxiv>=2.0.0

# --- Vector Search (Memory) ---
faiss-cpu~=1.13.2              # Vector similarity search

# --- CLI & Logging ---
rich~=15.0.0                   # Rich text and formatting
tqdm~=4.67.3                   # Progress bars

# --- RL Training (Phase 3+) ---
# trl>=0.12.0
# verl>=0.4.0
# torch>=2.4.0
# accelerate>=0.34.0

# --- Inference (Phase 1+) ---
# vllm>=0.6.0

# --- Evaluation (Phase 4+) ---
# scipy>=1.13.0
# scikit-learn>=1.5.0
# pandas>=2.2.0
# matplotlib>=3.9.0

# --- Development ---
# pytest>=8.0.0
# pytest-asyncio>=0.24.0
# ruff>=0.6.0
# mypy>=1.11.0
